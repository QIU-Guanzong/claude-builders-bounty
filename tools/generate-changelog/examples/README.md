# Sample provenance

`sample-CHANGELOG.md` was generated from the public repository
[`siddhss5/labdata`](https://github.com/siddhss5/labdata) at commit
`45c9c8615e6a16246bdd9ea99d8b2d55fa9afbee`. The latest reachable Git tag at
that revision was `schema-v4`.

The repository's current default branch was cloned on 2026-09-25, then checked
out at that public historical commit so the sample demonstrates changes after
a real tag. It was generated with:

```sh
bash tools/generate-changelog/changelog.sh --repo /path/to/labdata --stdout
```

The output is a checked-in sample, not a claim that this tool generated the
upstream project's published changelog.
